export const runSimulation = async (data) => {
    // Placeholder for API call to /api/simulate
    console.log("Running simulation with data:", data);
    return null;
};

export const runComparison = async (data) => {
    // Placeholder for API call to /api/compare
    console.log("Running comparison with data:", data);
    return null;
};
