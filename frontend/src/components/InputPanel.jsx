import React from 'react';

const InputPanel = () => {
    return (
        <div id="input-panel">
            {/* Input controls for disk requests */}
        </div>
    );
};

export default InputPanel;
