import React from 'react';

const AlgorithmSelector = () => {
    return (
        <div id="algorithm-selector">
            {/* Selection for scheduling algorithms */}
        </div>
    );
};

export default AlgorithmSelector;
